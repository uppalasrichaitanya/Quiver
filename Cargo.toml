[workspace]
members = [
    "quiver-core",
    "quiver-server",
    "quiver-py",
]
exclude = ["fuzz"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/uppalasrichaitanya/Quiver"
description = "A portfolio-grade, single-node vector search engine implementing mmap storage and HNSW from scratch"

[workspace.dependencies]
# Core dependencies
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
memmap2 = "0.9"
crc32fast = "1.4"
byteorder = "1.5"
thiserror = "2"
rand = "0.9"

# Benchmarking
criterion = { version = "0.5", features = ["html_reports"] }

# Serialization (for benchmark output, config)
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Server
axum = "0.8"
tokio = { version = "1", features = ["full"] }

# Python bindings
pyo3 = { version = "0.23", features = ["extension-module"] }
